<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ranyk.spring.ai.rag.knowledge.database.repository.chat.message.ChatMessageRepository">
    <resultMap id="BaseResultMap"
               type="com.ranyk.spring.ai.rag.knowledge.database.domain.chat.message.entity.ChatMessage">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="session_id" jdbcType="BIGINT" property="sessionId"/>
        <result column="role" jdbcType="VARCHAR" property="role"/>
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
        <result column="refs" jdbcType="LONGVARCHAR" property="refs"/>
        <result column="create_by" jdbcType="BIGINT" property="createBy"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_by" jdbcType="BIGINT" property="updateBy"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <!-- 统计近7日每日助手回复消息数量 -->
    <select id="countAssistantByDayLast7" resultType="map">
        SELECT DATE_FORMAT(DATE(create_time), '%Y-%m-%d') AS dayKey, COUNT(*) AS cnt
        FROM t_chat_message
        WHERE role = 'ASSISTANT'
          AND create_time >= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE_FORMAT(DATE(create_time), '%Y-%m-%d')
        ORDER BY dayKey
    </select>
</mapper>